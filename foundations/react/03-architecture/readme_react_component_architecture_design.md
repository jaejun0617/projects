# React Component Architecture Design (실무 설계 기준)

> 이 문서는 컴포넌트를 어떻게 "나누는지"를 설명하지 않는다.  
> **왜 그렇게 나눌 수밖에 없는지**를 설계 관점에서 설명한다.

---

## 🧭 이 챕터의 위치

- React Rendering & Performance Design **이후**
- 실전 React 프로젝트 **시작 전 필수 단계**

즉,
- 컴포넌트 분리 요령 ❌
- **구조가 망가지지 않는 기준**을 만드는 단계 ✅

---

## 🎯 목표

- 컴포넌트의 책임을 명확히 구분한다
- 상태 위치를 합리적으로 결정한다
- 확장 가능한 트리 구조를 설계한다

---

## 1. 컴포넌트란 무엇인가 (재정의)

### 잘못된 정의

- JSX 덩어리
- 재사용용 UI 조각

### 실무적 정의

> **하나의 책임을 가지는 UI 계산 단위**

- 상태를 가질 수도, 안 가질 수도 있다
- 책임이 흐려지면 재사용성은 사라진다

---

## 2. 책임이 섞일 때 생기는 문제

```js
function Page() {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetchData().then(setData);
  }, []);

  return <List data={data} />;
}
```

### 문제점

- 데이터 획득
- 상태 관리
- 렌더링

👉 모든 책임이 한 컴포넌트에 집중됨

---

## 3. 관심사의 분리 (Separation of Concerns)

### 기준

- 데이터 흐름과 UI 표현 분리
- 상태와 표시 분리

```txt
Container → State / Logic
Presentational → UI
```

---

## 4. Container / Presentational 패턴

### Container

- 상태 보유
- 비즈니스 로직
- fetch / effect

### Presentational

- props 기반 렌더링
- 상태 없음 또는 최소화

👉 역할이 명확해진다

---

## 5. 상태는 어디에 둬야 하는가

### 원칙

> **상태를 가장 낮은 공통 조상에 둔다**

- 너무 위 ❌ → 리렌더 범위 증가
- 너무 아래 ❌ → 상태 공유 불가

---

## 6. Props Drilling의 오해

### 오해

- props가 많으면 나쁘다

### 실제 문제

- 의미 없는 중계
- 책임 없는 컴포넌트

👉 구조 문제이지 props 개수 문제가 아님

---

## 7. 컴포넌트 분리 기준 체크리스트

- [ ] 이 컴포넌트는 하나의 이유로만 변경되는가?
- [ ] 상태 변경이 이 컴포넌트의 책임인가?
- [ ] 이 UI는 다른 상태에서도 재사용 가능한가?

---

## 8. 폴더 구조 사고 체계

### 역할 중심 구조

```txt
components/
 ├─ UserList/
 │   ├─ UserList.container.jsx
 │   └─ UserList.view.jsx
```

- 파일 분리는 결과
- **역할 분리가 먼저**

---

## 9. 성능과 구조의 관계

- 구조가 좋으면 최적화가 필요 없다
- memo는 구조 실패를 숨기는 도구가 아니다

👉 구조 → 성능

---

## 🎯 이 챕터를 마치면

- 컴포넌트를 감각으로 나누지 않는다
- 상태 위치를 설명할 수 있다
- 대규모 트리도 두렵지 않다

---

## 다음 챕터 예고

> **React State Management Design**  
> 상태가 커질 때 무엇이 달라지는가

---

**이 문서는 React 구조 설계 기준 문서다.**