# Day 26 — Bulk Select / Bulk Delete

## ✅ 한 줄 요약
여러 UI 조작을 한 번에 처리하는 문제는 **이벤트 문제가 아니라 상태 연산 문제**다.

---

## 📌 프로젝트 개요 (WHY)
Day 26은 체크박스를 여러 개 다루는 날이 아니다.  
**여러 개의 상태를 동시에 제어할 수 있는 구조를 설계하는 단계**다.

단일 선택 → 다중 선택으로 넘어오면서,  
프론트엔드의 난이도는 UI가 아니라 **상태 모델링**에서 급격히 상승한다.

이 Day의 목적은 다음 질문에 답하는 것이다.

> “여러 개가 선택된 상태를, 어떻게 하나의 기준으로 판단하고 조작할 것인가?”

---

## 🎯 미션 목표
- 리스트 항목 각각에 **선택 상태(selected)**를 부여한다
- 선택된 항목이 하나라도 있으면 **Bulk Delete 버튼을 활성화**한다
- 전체 선택 / 전체 해제 / 선택 삭제를 **상태 연산으로 처리**한다
- DOM 조작이 아닌 **state → render 흐름을 유지**한다

---

## 🧠 핵심 사고
### 1. Bulk는 ‘기능’이 아니라 ‘상태 계산’
- 여러 개를 삭제한다 ❌  
- **선택된 상태를 가진 항목을 제거한다 ⭕**

UI 액션은 항상 상태 연산으로 환원된다.

### 2. 선택 여부는 별도의 상태가 아니다
```js
{ id: 1, text: 'Learn JS', selected: false }
```
- 체크 여부를 DOM에서 읽지 않는다  
- **항목 자체가 자신의 선택 상태를 가진다**  
- Bulk 연산은 배열 메서드로 해결된다

### 3. 버튼 활성화는 조건문이 아니라 결과값
```js
const hasSelected = items.some(item => item.selected);
```
- if로 분기하지 않는다  
- 값 계산 결과로 UI를 제어한다

---

## 🧩 상태 모델
```js
let items = [
  { id: 1, text: 'Learn JS', selected: false },
  { id: 2, text: 'Practice DOM', selected: false },
  { id: 3, text: 'Build State UI', selected: false },
];
```

---

## ⚙️ 구현 기준 & 기른 역량
- 모든 변경은 `items` 상태 변경으로만 수행
- `map / filter / some` 중심 사고
- render는 항상 상태의 결과

---

## 📂 파일 구조
```
26-bulk-select-delete/
├─ index.html
├─ css/
│  └─ style.css
└─ js/
   └─ main.js
```

---

## 🎯 얻어가는 점
- Bulk 기능은 상태 설계 문제
- Day 27(LocalStorage)로 자연스럽게 확장 가능
