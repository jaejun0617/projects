# Day 51 — Simple Router 구현 (프레임워크 없이)

## 🏷 Topic
Client-side Router / History API / Route Table / SPA Navigation

## 🔎 관련 검색어
- javascript simple router implementation
- client side routing without framework
- history api router pattern
- spa router basics
- react router principle

---

## ✅ 한 줄 요약
Router는 라이브러리가 아니라  
**URL 상태를 View로 매핑하는 규칙 집합**이다.

---

## 📌 프로젝트 개요 (WHY)
Day 51은 React Router를 쓰는 날이 아니다.  
**Router가 내부적으로 무엇을 하고 있는지 직접 만들어보는 단계**다.

Router의 본질은 단순하다.

- URL을 읽고
- 어떤 화면을 보여줄지 결정하고
- 뒤로가기/새로고침에도 일관성을 유지한다

이 Day에서는 그 최소 구조를 구현한다.

---

## 🎯 미션 목표
- route table 기반 화면 전환 구현
- 없는 경로에 대한 404 처리
- URL 직접 진입 및 새로고침 대응
- History API와 상태 동기화

---

## 🧠 핵심 사고

### 1️⃣ Router = 상태 관리자
```js
let state = { route: '/' };
```

- Router는 현재 route 상태를 소유
- View는 route의 결과일 뿐

---

### 2️⃣ Route Table의 역할
```js
const routes = {
  '/': Home,
  '/todos': Todos,
};
```

- URL → View 매핑
- if/switch 제거
- 확장 가능한 구조

---

### 3️⃣ `<a>`는 UX, 이동은 JS
```js
e.preventDefault();
navigate(route);
```

- 서버 이동 차단
- SPA 흐름 유지
- 접근성은 `<a>`로 확보

---

## 🧠 핵심 이론 보강

### 서버 라우팅 vs 클라이언트 라우팅
| 구분 | 서버 | 클라이언트 |
|---|---|---|
| 처리 주체 | 서버 | JS |
| 새로고침 | 정상 | fallback 필요 |
| 화면 전환 | 페이지 | View |

---

### 404는 Router 책임
```js
view ? view() : render404();
```

- 서버에 맡기지 않음
- SPA 내부 상태로 처리

---

## 🧩 상태 모델
```js
{
  route: '/'
}
```

- 단일 상태
- URL과 항상 동기화

---

## 🧠 핵심 코드 스냅샷

### 1️⃣ 이동 처리
```js
function navigate(route) {
  history.pushState({ route }, '', route);
  state = { route };
  render();
}
```

---

### 2️⃣ 뒤로가기 대응
```js
window.addEventListener('popstate', ...)
```

---

### 3️⃣ 초기 진입 처리
```js
history.replaceState(...)
```

---

## ⚙️ 구현 기준 & 기른 역량

### 구현 기준
- URL = State 원칙 유지
- Router가 View를 직접 결정
- DOM 직접 이동 제거

### 기른 역량
- SPA 라우팅 구조 이해
- History API 실전 활용
- React Router 내부 동작 이해 기반

---

## 📂 파일 구조
```
51-simple-router/
├─ index.html
├─ css/
│  └─ style.css
└─ js/
   └─ main.js
```

---

## ☑️ 체크리스트
- [ ] Router의 역할을 설명할 수 있는가
- [ ] route table 구조를 이해했는가
- [ ] 뒤로가기/새로고침이 동작하는가
- [ ] 404 처리가 Router 내부에서 되는가

---

## 🎯 얻어가는 점
- Router는 개념이다
- URL은 상태다
- 라이브러리는 선택지일 뿐
- SPA의 핵심 뼈대를 직접 만들었다
